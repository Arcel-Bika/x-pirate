import { ProcessedLead } from '../types/mission';

export async function storeInAirtable(lead: ProcessedLead): Promise<string> {
  // Implémentation du stockage Airtable simulé
}

export async function sendNotification(lead: ProcessedLead): Promise<boolean> {
  // Implémentation de l'envoi de notification simulé
}

